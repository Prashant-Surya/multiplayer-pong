var socket = io('http://localhost');
function guid() {
  function s4() {
    return Math.floor((1 + Math.random()) * 0x10000)
      .toString(16)
      .substring(1);
  }
  return s4() + s4() + '-' + s4() + '-' + s4() + '-' +
    s4() + '-' + s4() + s4() + s4();
}

var player_id = guid();
socket.emit('connect', {'player_name': player_name, 'player_id' : player_id});